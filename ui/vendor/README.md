# Visual Hive notation converter

The `visual_hive` package supplies the viewer's Analytic notation conversion.
Included with permission. No separate upstream license was supplied.

The Rust rules helper controls legal moves, history, repetition and outcomes.
The converter only translates notation.
